# Default values for kubeform-provider-aws.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Pass values to kubeform-provider chart
kubeform-provider: # +doc-gen:break
  operator:
    tag: v0.1.0
  provider:
    name: aws

crds:
  # If true, installs the kubeform-provider-aws-accessanalyzer-crds chart
  accessanalyzer: false
  # If true, installs the kubeform-provider-aws-acm-crds chart
  acm: false
  # If true, installs the kubeform-provider-aws-acmpca-crds chart
  acmpca: false
  # If true, installs the kubeform-provider-aws-alb-crds chart
  alb: false
  # If true, installs the kubeform-provider-aws-ami-crds chart
  ami: false
  # If true, installs the kubeform-provider-aws-amplify-crds chart
  amplify: false
  # If true, installs the kubeform-provider-aws-apigateway-crds chart
  apigateway: false
  # If true, installs the kubeform-provider-aws-apigatewayv2-crds chart
  apigatewayv2: false
  # If true, installs the kubeform-provider-aws-app-crds chart
  app: false
  # If true, installs the kubeform-provider-aws-appautoscaling-crds chart
  appautoscaling: false
  # If true, installs the kubeform-provider-aws-appmesh-crds chart
  appmesh: false
  # If true, installs the kubeform-provider-aws-apprunner-crds chart
  apprunner: false
  # If true, installs the kubeform-provider-aws-appsync-crds chart
  appsync: false
  # If true, installs the kubeform-provider-aws-athena-crds chart
  athena: false
  # If true, installs the kubeform-provider-aws-autoscaling-crds chart
  autoscaling: false
  # If true, installs the kubeform-provider-aws-autoscalingplans-crds chart
  autoscalingplans: false
  # If true, installs the kubeform-provider-aws-backup-crds chart
  backup: false
  # If true, installs the kubeform-provider-aws-batch-crds chart
  batch: false
  # If true, installs the kubeform-provider-aws-budgets-crds chart
  budgets: false
  # If true, installs the kubeform-provider-aws-cloud9-crds chart
  cloud9: false
  # If true, installs the kubeform-provider-aws-cloudformationstack-crds chart
  cloudformationstack: false
  # If true, installs the kubeform-provider-aws-cloudformationtype-crds chart
  cloudformationtype: false
  # If true, installs the kubeform-provider-aws-cloudfront-crds chart
  cloudfront: false
  # If true, installs the kubeform-provider-aws-cloudhsmv2-crds chart
  cloudhsmv2: false
  # If true, installs the kubeform-provider-aws-cloudtrail-crds chart
  cloudtrail: false
  # If true, installs the kubeform-provider-aws-cloudwatch-crds chart
  cloudwatch: false
  # If true, installs the kubeform-provider-aws-codeartifact-crds chart
  codeartifact: false
  # If true, installs the kubeform-provider-aws-codebuild-crds chart
  codebuild: false
  # If true, installs the kubeform-provider-aws-codecommit-crds chart
  codecommit: false
  # If true, installs the kubeform-provider-aws-codedeploy-crds chart
  codedeploy: false
  # If true, installs the kubeform-provider-aws-codepipeline-crds chart
  codepipeline: false
  # If true, installs the kubeform-provider-aws-codestarconnections-crds chart
  codestarconnections: false
  # If true, installs the kubeform-provider-aws-codestarnotifications-crds chart
  codestarnotifications: false
  # If true, installs the kubeform-provider-aws-cognito-crds chart
  cognito: false
  # If true, installs the kubeform-provider-aws-config-crds chart
  config: false
  # If true, installs the kubeform-provider-aws-cur-crds chart
  cur: false
  # If true, installs the kubeform-provider-aws-customer-crds chart
  customer: false
  # If true, installs the kubeform-provider-aws-datapipeline-crds chart
  datapipeline: false
  # If true, installs the kubeform-provider-aws-datasync-crds chart
  datasync: false
  # If true, installs the kubeform-provider-aws-dax-crds chart
  dax: false
  # If true, installs the kubeform-provider-aws-db-crds chart
  db: false
  # If true, installs the kubeform-provider-aws-default-crds chart
  default: false
  # If true, installs the kubeform-provider-aws-devicefarm-crds chart
  devicefarm: false
  # If true, installs the kubeform-provider-aws-directoryservice-crds chart
  directoryservice: false
  # If true, installs the kubeform-provider-aws-dlm-crds chart
  dlm: false
  # If true, installs the kubeform-provider-aws-dms-crds chart
  dms: false
  # If true, installs the kubeform-provider-aws-docdb-crds chart
  docdb: false
  # If true, installs the kubeform-provider-aws-dx-crds chart
  dx: false
  # If true, installs the kubeform-provider-aws-dynamodb-crds chart
  dynamodb: false
  # If true, installs the kubeform-provider-aws-ebs-crds chart
  ebs: false
  # If true, installs the kubeform-provider-aws-ec2-crds chart
  ec2: false
  # If true, installs the kubeform-provider-aws-ecr-crds chart
  ecr: false
  # If true, installs the kubeform-provider-aws-ecrpublic-crds chart
  ecrpublic: false
  # If true, installs the kubeform-provider-aws-ecs-crds chart
  ecs: false
  # If true, installs the kubeform-provider-aws-efs-crds chart
  efs: false
  # If true, installs the kubeform-provider-aws-egress-crds chart
  egress: false
  # If true, installs the kubeform-provider-aws-eip-crds chart
  eip: false
  # If true, installs the kubeform-provider-aws-eks-crds chart
  eks: false
  # If true, installs the kubeform-provider-aws-elasticache-crds chart
  elasticache: false
  # If true, installs the kubeform-provider-aws-elasticbeanstalk-crds chart
  elasticbeanstalk: false
  # If true, installs the kubeform-provider-aws-elasticsearchdomain-crds chart
  elasticsearchdomain: false
  # If true, installs the kubeform-provider-aws-elastictranscoder-crds chart
  elastictranscoder: false
  # If true, installs the kubeform-provider-aws-elb-crds chart
  elb: false
  # If true, installs the kubeform-provider-aws-emr-crds chart
  emr: false
  # If true, installs the kubeform-provider-aws-flow-crds chart
  flow: false
  # If true, installs the kubeform-provider-aws-fms-crds chart
  fms: false
  # If true, installs the kubeform-provider-aws-fsx-crds chart
  fsx: false
  # If true, installs the kubeform-provider-aws-gamelift-crds chart
  gamelift: false
  # If true, installs the kubeform-provider-aws-glaciervault-crds chart
  glaciervault: false
  # If true, installs the kubeform-provider-aws-globalaccelerator-crds chart
  globalaccelerator: false
  # If true, installs the kubeform-provider-aws-glue-crds chart
  glue: false
  # If true, installs the kubeform-provider-aws-guardduty-crds chart
  guardduty: false
  # If true, installs the kubeform-provider-aws-iam-crds chart
  iam: false
  # If true, installs the kubeform-provider-aws-imagebuilder-crds chart
  imagebuilder: false
  # If true, installs the kubeform-provider-aws-inspector-crds chart
  inspector: false
  # If true, installs the kubeform-provider-aws-instance-crds chart
  instance: false
  # If true, installs the kubeform-provider-aws-internet-crds chart
  internet: false
  # If true, installs the kubeform-provider-aws-iot-crds chart
  iot: false
  # If true, installs the kubeform-provider-aws-key-crds chart
  key: false
  # If true, installs the kubeform-provider-aws-kinesis-crds chart
  kinesis: false
  # If true, installs the kubeform-provider-aws-kinesisanalyticsv2-crds chart
  kinesisanalyticsv2: false
  # If true, installs the kubeform-provider-aws-kms-crds chart
  kms: false
  # If true, installs the kubeform-provider-aws-lakeformation-crds chart
  lakeformation: false
  # If true, installs the kubeform-provider-aws-lambda-crds chart
  lambda: false
  # If true, installs the kubeform-provider-aws-launch-crds chart
  launch: false
  # If true, installs the kubeform-provider-aws-lb-crds chart
  lb: false
  # If true, installs the kubeform-provider-aws-lex-crds chart
  lex: false
  # If true, installs the kubeform-provider-aws-licensemanager-crds chart
  licensemanager: false
  # If true, installs the kubeform-provider-aws-lightsail-crds chart
  lightsail: false
  # If true, installs the kubeform-provider-aws-loadbalancer-crds chart
  loadbalancer: false
  # If true, installs the kubeform-provider-aws-macie-crds chart
  macie: false
  # If true, installs the kubeform-provider-aws-macie2-crds chart
  macie2: false
  # If true, installs the kubeform-provider-aws-main-crds chart
  main: false
  # If true, installs the kubeform-provider-aws-media-crds chart
  media: false
  # If true, installs the kubeform-provider-aws-mq-crds chart
  mq: false
  # If true, installs the kubeform-provider-aws-msk-crds chart
  msk: false
  # If true, installs the kubeform-provider-aws-mwaa-crds chart
  mwaa: false
  # If true, installs the kubeform-provider-aws-nat-crds chart
  nat: false
  # If true, installs the kubeform-provider-aws-neptune-crds chart
  neptune: false
  # If true, installs the kubeform-provider-aws-network-crds chart
  network: false
  # If true, installs the kubeform-provider-aws-networkfirewall-crds chart
  networkfirewall: false
  # If true, installs the kubeform-provider-aws-opsworks-crds chart
  opsworks: false
  # If true, installs the kubeform-provider-aws-organizations-crds chart
  organizations: false
  # If true, installs the kubeform-provider-aws-pinpoint-crds chart
  pinpoint: false
  # If true, installs the kubeform-provider-aws-placement-crds chart
  placement: false
  # If true, installs the kubeform-provider-aws-prometheus-crds chart
  prometheus: false
  # If true, installs the kubeform-provider-aws-proxy-crds chart
  proxy: false
  # If true, installs the kubeform-provider-aws-qldb-crds chart
  qldb: false
  # If true, installs the kubeform-provider-aws-quicksight-crds chart
  quicksight: false
  # If true, installs the kubeform-provider-aws-ram-crds chart
  ram: false
  # If true, installs the kubeform-provider-aws-rds-crds chart
  rds: false
  # If true, installs the kubeform-provider-aws-redshift-crds chart
  redshift: false
  # If true, installs the kubeform-provider-aws-resourcegroups-crds chart
  resourcegroups: false
  # If true, installs the kubeform-provider-aws-route-crds chart
  route: false
  # If true, installs the kubeform-provider-aws-route53-crds chart
  route53: false
  # If true, installs the kubeform-provider-aws-s3-crds chart
  s3: false
  # If true, installs the kubeform-provider-aws-s3control-crds chart
  s3control: false
  # If true, installs the kubeform-provider-aws-s3outposts-crds chart
  s3outposts: false
  # If true, installs the kubeform-provider-aws-sagemaker-crds chart
  sagemaker: false
  # If true, installs the kubeform-provider-aws-schemas-crds chart
  schemas: false
  # If true, installs the kubeform-provider-aws-secretsmanager-crds chart
  secretsmanager: false
  # If true, installs the kubeform-provider-aws-security-crds chart
  security: false
  # If true, installs the kubeform-provider-aws-securityhub-crds chart
  securityhub: false
  # If true, installs the kubeform-provider-aws-serverlessapplicationrepository-crds chart
  serverlessapplicationrepository: false
  # If true, installs the kubeform-provider-aws-servicecatalog-crds chart
  servicecatalog: false
  # If true, installs the kubeform-provider-aws-servicediscovery-crds chart
  servicediscovery: false
  # If true, installs the kubeform-provider-aws-servicequotas-crds chart
  servicequotas: false
  # If true, installs the kubeform-provider-aws-ses-crds chart
  ses: false
  # If true, installs the kubeform-provider-aws-sfn-crds chart
  sfn: false
  # If true, installs the kubeform-provider-aws-shield-crds chart
  shield: false
  # If true, installs the kubeform-provider-aws-signer-crds chart
  signer: false
  # If true, installs the kubeform-provider-aws-simpledb-crds chart
  simpledb: false
  # If true, installs the kubeform-provider-aws-snapshot-crds chart
  snapshot: false
  # If true, installs the kubeform-provider-aws-sns-crds chart
  sns: false
  # If true, installs the kubeform-provider-aws-spot-crds chart
  spot: false
  # If true, installs the kubeform-provider-aws-sqsqueue-crds chart
  sqsqueue: false
  # If true, installs the kubeform-provider-aws-ssm-crds chart
  ssm: false
  # If true, installs the kubeform-provider-aws-ssoadmin-crds chart
  ssoadmin: false
  # If true, installs the kubeform-provider-aws-storagegateway-crds chart
  storagegateway: false
  # If true, installs the kubeform-provider-aws-subnet-crds chart
  subnet: false
  # If true, installs the kubeform-provider-aws-swf-crds chart
  swf: false
  # If true, installs the kubeform-provider-aws-synthetics-crds chart
  synthetics: false
  # If true, installs the kubeform-provider-aws-timestreamwrite-crds chart
  timestreamwrite: false
  # If true, installs the kubeform-provider-aws-transfer-crds chart
  transfer: false
  # If true, installs the kubeform-provider-aws-volume-crds chart
  volume: false
  # If true, installs the kubeform-provider-aws-vpc-crds chart
  vpc: false
  # If true, installs the kubeform-provider-aws-vpn-crds chart
  vpn: false
  # If true, installs the kubeform-provider-aws-waf-crds chart
  waf: false
  # If true, installs the kubeform-provider-aws-wafregional-crds chart
  wafregional: false
  # If true, installs the kubeform-provider-aws-wafv2-crds chart
  wafv2: false
  # If true, installs the kubeform-provider-aws-worklink-crds chart
  worklink: false
  # If true, installs the kubeform-provider-aws-workspaces-crds chart
  workspaces: false
  # If true, installs the kubeform-provider-aws-xray-crds chart
  xray: false
