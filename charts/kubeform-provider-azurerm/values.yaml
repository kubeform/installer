# Default values for kubeform-provider-azurerm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Pass values to kubeform-provider chart
kubeform-provider: # +doc-gen:break
  operator:
    tag: v0.3.0
  provider:
    name: azurerm

crds:
  # If true, installs the kubeform-provider-azurerm-advanced-crds chart
  advanced: false
  # If true, installs the kubeform-provider-azurerm-analysis-crds chart
  analysis: false
  # If true, installs the kubeform-provider-azurerm-apimanagement-crds chart
  apimanagement: false
  # If true, installs the kubeform-provider-azurerm-app-crds chart
  app: false
  # If true, installs the kubeform-provider-azurerm-application-crds chart
  application: false
  # If true, installs the kubeform-provider-azurerm-attestation-crds chart
  attestation: false
  # If true, installs the kubeform-provider-azurerm-automation-crds chart
  automation: false
  # If true, installs the kubeform-provider-azurerm-availability-crds chart
  availability: false
  # If true, installs the kubeform-provider-azurerm-backup-crds chart
  backup: false
  # If true, installs the kubeform-provider-azurerm-bastion-crds chart
  bastion: false
  # If true, installs the kubeform-provider-azurerm-batch-crds chart
  batch: false
  # If true, installs the kubeform-provider-azurerm-blueprint-crds chart
  blueprint: false
  # If true, installs the kubeform-provider-azurerm-bot-crds chart
  bot: false
  # If true, installs the kubeform-provider-azurerm-cdn-crds chart
  cdn: false
  # If true, installs the kubeform-provider-azurerm-cognitive-crds chart
  cognitive: false
  # If true, installs the kubeform-provider-azurerm-communication-crds chart
  communication: false
  # If true, installs the kubeform-provider-azurerm-consumption-crds chart
  consumption: false
  # If true, installs the kubeform-provider-azurerm-container-crds chart
  container: false
  # If true, installs the kubeform-provider-azurerm-cosmosdb-crds chart
  cosmosdb: false
  # If true, installs the kubeform-provider-azurerm-cost-crds chart
  cost: false
  # If true, installs the kubeform-provider-azurerm-custom-crds chart
  custom: false
  # If true, installs the kubeform-provider-azurerm-dashboard-crds chart
  dashboard: false
  # If true, installs the kubeform-provider-azurerm-data-crds chart
  data: false
  # If true, installs the kubeform-provider-azurerm-database-crds chart
  database: false
  # If true, installs the kubeform-provider-azurerm-databox-crds chart
  databox: false
  # If true, installs the kubeform-provider-azurerm-databricks-crds chart
  databricks: false
  # If true, installs the kubeform-provider-azurerm-dedicatedhardware-crds chart
  dedicatedhardware: false
  # If true, installs the kubeform-provider-azurerm-dedicatedhost-crds chart
  dedicatedhost: false
  # If true, installs the kubeform-provider-azurerm-devspace-crds chart
  devspace: false
  # If true, installs the kubeform-provider-azurerm-devtest-crds chart
  devtest: false
  # If true, installs the kubeform-provider-azurerm-digital-crds chart
  digital: false
  # If true, installs the kubeform-provider-azurerm-disk-crds chart
  disk: false
  # If true, installs the kubeform-provider-azurerm-dns-crds chart
  dns: false
  # If true, installs the kubeform-provider-azurerm-eventgrid-crds chart
  eventgrid: false
  # If true, installs the kubeform-provider-azurerm-eventhub-crds chart
  eventhub: false
  # If true, installs the kubeform-provider-azurerm-expressroute-crds chart
  expressroute: false
  # If true, installs the kubeform-provider-azurerm-firewall-crds chart
  firewall: false
  # If true, installs the kubeform-provider-azurerm-frontdoor-crds chart
  frontdoor: false
  # If true, installs the kubeform-provider-azurerm-function-crds chart
  function: false
  # If true, installs the kubeform-provider-azurerm-hdinsight-crds chart
  hdinsight: false
  # If true, installs the kubeform-provider-azurerm-healthbot-crds chart
  healthbot: false
  # If true, installs the kubeform-provider-azurerm-healthcare-crds chart
  healthcare: false
  # If true, installs the kubeform-provider-azurerm-hpc-crds chart
  hpc: false
  # If true, installs the kubeform-provider-azurerm-image-crds chart
  image: false
  # If true, installs the kubeform-provider-azurerm-integration-crds chart
  integration: false
  # If true, installs the kubeform-provider-azurerm-iotcentral-crds chart
  iotcentral: false
  # If true, installs the kubeform-provider-azurerm-iothub-crds chart
  iothub: false
  # If true, installs the kubeform-provider-azurerm-iotsecurity-crds chart
  iotsecurity: false
  # If true, installs the kubeform-provider-azurerm-iottime-crds chart
  iottime: false
  # If true, installs the kubeform-provider-azurerm-ip-crds chart
  ip: false
  # If true, installs the kubeform-provider-azurerm-keyvault-crds chart
  keyvault: false
  # If true, installs the kubeform-provider-azurerm-kubernetescluster-crds chart
  kubernetescluster: false
  # If true, installs the kubeform-provider-azurerm-kusto-crds chart
  kusto: false
  # If true, installs the kubeform-provider-azurerm-lb-crds chart
  lb: false
  # If true, installs the kubeform-provider-azurerm-lighthouse-crds chart
  lighthouse: false
  # If true, installs the kubeform-provider-azurerm-linux-crds chart
  linux: false
  # If true, installs the kubeform-provider-azurerm-local-crds chart
  local: false
  # If true, installs the kubeform-provider-azurerm-loganalytics-crds chart
  loganalytics: false
  # If true, installs the kubeform-provider-azurerm-logicapp-crds chart
  logicapp: false
  # If true, installs the kubeform-provider-azurerm-machine-crds chart
  machine: false
  # If true, installs the kubeform-provider-azurerm-maintenance-crds chart
  maintenance: false
  # If true, installs the kubeform-provider-azurerm-managed-crds chart
  managed: false
  # If true, installs the kubeform-provider-azurerm-management-crds chart
  management: false
  # If true, installs the kubeform-provider-azurerm-maps-crds chart
  maps: false
  # If true, installs the kubeform-provider-azurerm-mariadb-crds chart
  mariadb: false
  # If true, installs the kubeform-provider-azurerm-marketplace-crds chart
  marketplace: false
  # If true, installs the kubeform-provider-azurerm-media-crds chart
  media: false
  # If true, installs the kubeform-provider-azurerm-monitor-crds chart
  monitor: false
  # If true, installs the kubeform-provider-azurerm-mssql-crds chart
  mssql: false
  # If true, installs the kubeform-provider-azurerm-mysql-crds chart
  mysql: false
  # If true, installs the kubeform-provider-azurerm-nat-crds chart
  nat: false
  # If true, installs the kubeform-provider-azurerm-netapp-crds chart
  netapp: false
  # If true, installs the kubeform-provider-azurerm-network-crds chart
  network: false
  # If true, installs the kubeform-provider-azurerm-notificationhub-crds chart
  notificationhub: false
  # If true, installs the kubeform-provider-azurerm-orchestrated-crds chart
  orchestrated: false
  # If true, installs the kubeform-provider-azurerm-packet-crds chart
  packet: false
  # If true, installs the kubeform-provider-azurerm-point-crds chart
  point: false
  # If true, installs the kubeform-provider-azurerm-policy-crds chart
  policy: false
  # If true, installs the kubeform-provider-azurerm-postgresql-crds chart
  postgresql: false
  # If true, installs the kubeform-provider-azurerm-powerbi-crds chart
  powerbi: false
  # If true, installs the kubeform-provider-azurerm-private-crds chart
  private: false
  # If true, installs the kubeform-provider-azurerm-proximity-crds chart
  proximity: false
  # If true, installs the kubeform-provider-azurerm-publicip-crds chart
  publicip: false
  # If true, installs the kubeform-provider-azurerm-purview-crds chart
  purview: false
  # If true, installs the kubeform-provider-azurerm-recovery-crds chart
  recovery: false
  # If true, installs the kubeform-provider-azurerm-redis-crds chart
  redis: false
  # If true, installs the kubeform-provider-azurerm-relay-crds chart
  relay: false
  # If true, installs the kubeform-provider-azurerm-resource-crds chart
  resource: false
  # If true, installs the kubeform-provider-azurerm-role-crds chart
  role: false
  # If true, installs the kubeform-provider-azurerm-route-crds chart
  route: false
  # If true, installs the kubeform-provider-azurerm-search-crds chart
  search: false
  # If true, installs the kubeform-provider-azurerm-security-crds chart
  security: false
  # If true, installs the kubeform-provider-azurerm-sentinel-crds chart
  sentinel: false
  # If true, installs the kubeform-provider-azurerm-service-crds chart
  service: false
  # If true, installs the kubeform-provider-azurerm-servicebus-crds chart
  servicebus: false
  # If true, installs the kubeform-provider-azurerm-sharedimage-crds chart
  sharedimage: false
  # If true, installs the kubeform-provider-azurerm-signalr-crds chart
  signalr: false
  # If true, installs the kubeform-provider-azurerm-siterecovery-crds chart
  siterecovery: false
  # If true, installs the kubeform-provider-azurerm-snapshot-crds chart
  snapshot: false
  # If true, installs the kubeform-provider-azurerm-spatial-crds chart
  spatial: false
  # If true, installs the kubeform-provider-azurerm-spring-crds chart
  spring: false
  # If true, installs the kubeform-provider-azurerm-sql-crds chart
  sql: false
  # If true, installs the kubeform-provider-azurerm-ssh-crds chart
  ssh: false
  # If true, installs the kubeform-provider-azurerm-stack-crds chart
  stack: false
  # If true, installs the kubeform-provider-azurerm-static-crds chart
  static: false
  # If true, installs the kubeform-provider-azurerm-storage-crds chart
  storage: false
  # If true, installs the kubeform-provider-azurerm-stream-crds chart
  stream: false
  # If true, installs the kubeform-provider-azurerm-subnet-crds chart
  subnet: false
  # If true, installs the kubeform-provider-azurerm-subscription-crds chart
  subscription: false
  # If true, installs the kubeform-provider-azurerm-synapse-crds chart
  synapse: false
  # If true, installs the kubeform-provider-azurerm-template-crds chart
  template: false
  # If true, installs the kubeform-provider-azurerm-tenant-crds chart
  tenant: false
  # If true, installs the kubeform-provider-azurerm-trafficmanager-crds chart
  trafficmanager: false
  # If true, installs the kubeform-provider-azurerm-user-crds chart
  user: false
  # If true, installs the kubeform-provider-azurerm-virtual-crds chart
  virtual: false
  # If true, installs the kubeform-provider-azurerm-vmware-crds chart
  vmware: false
  # If true, installs the kubeform-provider-azurerm-vpn-crds chart
  vpn: false
  # If true, installs the kubeform-provider-azurerm-web-crds chart
  web: false
  # If true, installs the kubeform-provider-azurerm-windows-crds chart
  windows: false
