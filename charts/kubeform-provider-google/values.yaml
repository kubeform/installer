# Default values for kubeform-provider-google.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Pass values to kubeform-provider chart
kubeform-provider: # +doc-gen:break
  operator:
    tag: v0.3.0
  provider:
    name: google

crds:
  # If true, installs the kubeform-provider-google-accesscontext-crds chart
  accesscontext: false
  # If true, installs the kubeform-provider-google-active-crds chart
  active: false
  # If true, installs the kubeform-provider-google-apigee-crds chart
  apigee: false
  # If true, installs the kubeform-provider-google-appengine-crds chart
  appengine: false
  # If true, installs the kubeform-provider-google-bigquery-crds chart
  bigquery: false
  # If true, installs the kubeform-provider-google-bigtable-crds chart
  bigtable: false
  # If true, installs the kubeform-provider-google-billingaccount-crds chart
  billingaccount: false
  # If true, installs the kubeform-provider-google-billingbudget-crds chart
  billingbudget: false
  # If true, installs the kubeform-provider-google-billingsubaccount-crds chart
  billingsubaccount: false
  # If true, installs the kubeform-provider-google-binaryauthorization-crds chart
  binaryauthorization: false
  # If true, installs the kubeform-provider-google-cloud-crds chart
  cloud: false
  # If true, installs the kubeform-provider-google-cloudbuild-crds chart
  cloudbuild: false
  # If true, installs the kubeform-provider-google-cloudfunctionsfunction-crds chart
  cloudfunctionsfunction: false
  # If true, installs the kubeform-provider-google-cloudiot-crds chart
  cloudiot: false
  # If true, installs the kubeform-provider-google-composer-crds chart
  composer: false
  # If true, installs the kubeform-provider-google-compute-crds chart
  compute: false
  # If true, installs the kubeform-provider-google-container-crds chart
  container: false
  # If true, installs the kubeform-provider-google-data-crds chart
  data: false
  # If true, installs the kubeform-provider-google-dataflow-crds chart
  dataflow: false
  # If true, installs the kubeform-provider-google-dataproc-crds chart
  dataproc: false
  # If true, installs the kubeform-provider-google-datastore-crds chart
  datastore: false
  # If true, installs the kubeform-provider-google-deployment-crds chart
  deployment: false
  # If true, installs the kubeform-provider-google-dialogflow-crds chart
  dialogflow: false
  # If true, installs the kubeform-provider-google-dns-crds chart
  dns: false
  # If true, installs the kubeform-provider-google-endpoints-crds chart
  endpoints: false
  # If true, installs the kubeform-provider-google-eventarc-crds chart
  eventarc: false
  # If true, installs the kubeform-provider-google-filestore-crds chart
  filestore: false
  # If true, installs the kubeform-provider-google-firestore-crds chart
  firestore: false
  # If true, installs the kubeform-provider-google-folder-crds chart
  folder: false
  # If true, installs the kubeform-provider-google-game-crds chart
  game: false
  # If true, installs the kubeform-provider-google-healthcare-crds chart
  healthcare: false
  # If true, installs the kubeform-provider-google-iap-crds chart
  iap: false
  # If true, installs the kubeform-provider-google-identity-crds chart
  identity: false
  # If true, installs the kubeform-provider-google-kms-crds chart
  kms: false
  # If true, installs the kubeform-provider-google-logging-crds chart
  logging: false
  # If true, installs the kubeform-provider-google-memcache-crds chart
  memcache: false
  # If true, installs the kubeform-provider-google-ml-crds chart
  ml: false
  # If true, installs the kubeform-provider-google-monitoring-crds chart
  monitoring: false
  # If true, installs the kubeform-provider-google-network-crds chart
  network: false
  # If true, installs the kubeform-provider-google-notebooks-crds chart
  notebooks: false
  # If true, installs the kubeform-provider-google-organization-crds chart
  organization: false
  # If true, installs the kubeform-provider-google-os-crds chart
  os: false
  # If true, installs the kubeform-provider-google-project-crds chart
  project: false
  # If true, installs the kubeform-provider-google-pubsub-crds chart
  pubsub: false
  # If true, installs the kubeform-provider-google-redis-crds chart
  redis: false
  # If true, installs the kubeform-provider-google-resource-crds chart
  resource: false
  # If true, installs the kubeform-provider-google-runtimeconfig-crds chart
  runtimeconfig: false
  # If true, installs the kubeform-provider-google-scc-crds chart
  scc: false
  # If true, installs the kubeform-provider-google-secret-crds chart
  secret: false
  # If true, installs the kubeform-provider-google-service-crds chart
  service: false
  # If true, installs the kubeform-provider-google-sourcereporepository-crds chart
  sourcereporepository: false
  # If true, installs the kubeform-provider-google-spanner-crds chart
  spanner: false
  # If true, installs the kubeform-provider-google-sql-crds chart
  sql: false
  # If true, installs the kubeform-provider-google-storage-crds chart
  storage: false
  # If true, installs the kubeform-provider-google-tags-crds chart
  tags: false
  # If true, installs the kubeform-provider-google-tpu-crds chart
  tpu: false
  # If true, installs the kubeform-provider-google-vertex-crds chart
  vertex: false
  # If true, installs the kubeform-provider-google-vpc-crds chart
  vpc: false
  # If true, installs the kubeform-provider-google-workflows-crds chart
  workflows: false
